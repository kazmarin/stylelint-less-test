# Тестовый репозиторий stylelint

Используется для обучения студентов.

Добавить в `package.json`:

```
"scripts": {
  "test": "gulp less:lint",
  "start": "gulp",
  "precommit": "npm test"
},
```

`npm i`

`npm start` 
