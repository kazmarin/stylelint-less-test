{
  "name": "stylelint-less-test",
  "version": "2.0.0",
  "description": "",
  "scripts": {
    "test": "./node_modules/.bin/stylelint --syntax less ./less/**/*.less",
    "lint-staged": "lint-staged",
    "stylelint-staged": "./node_modules/.bin/stylelint --syntax less"
  },
  "lint-staged": {
    "stylelint-staged": "./less/**/*.less"
  },
  "pre-commit": "lint-staged",
  "author": "Nikolay Gromov",
  "repository": {
    "type": "git",
    "url": "https://github.com/nicothin/stylelint-less-test.git"
  },
  "license": "MIT",
  "devDependencies": {
    "lint-staged": "^3.2.1",
    "pre-commit": "^1.1.3",
    "stylelint": "^7.5.0",
    "stylelint-declaration-use-variable": "^1.5.0"
  }
}
